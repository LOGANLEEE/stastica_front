import styled from 'styled-components';

export const Wrapper = styled.div`
	.list {
		display: flex;
		flex-wrap: wrap;

		.btn {
			background-color: #527b5b;
			color: silver;
			width: 50%;
		}
	}
`;
